{
  "name": "pdvconveniencia",
  "private": true,
  "scripts": {
    "build": "cd pdv/vite-project && npm ci --include=dev && npm run build && cd ../backend && npm ci --include=dev && npm run build"
  }
}
